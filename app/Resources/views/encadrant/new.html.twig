{% extends 'admin.html.twig' %}

{% block body %}
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    <div class="col-lg-6 col-lg-offset-1">
    <h1>Encadrant creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-md btn-primary" style="float: right;"/>
    {{ form_end(form) }}

    <ul class="list-unstyled">
        <li>
            <a href="{{ path('encadrant_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
{% endblock %}
