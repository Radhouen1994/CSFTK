{% extends 'admin.html.twig' %}

{% block body %}
    <div class="col-lg-6 col-lg-offset-1">
    <h1>Actualite edit</h1>
    {% form_theme edit_form 'bootstrap_3_horizontal_layout.html.twig' %}
    {{ form_start(edit_form)  }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="btn btn-md btn-primary" style="float:right; margin-left: 10px;"/>
    {{ form_end(edit_form) }}
        {{ form_start(delete_form) }}
        <input type="submit" value="Delete" class="btn btn-md btn-danger" style="float:right; ">
        {{ form_end(delete_form) }}
    <div class="row" style="padding: 10px;"></div>
    <ul class="list-unstyled">
        <li>
            <a href="{{ path('actualite_index') }}">Back to the list</a>
        </li>
        <div class="row" style="padding: 10px;"></div>
        <li>

        </li>
    </ul>
    </div>
{% endblock %}
