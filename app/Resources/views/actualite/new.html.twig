{% extends 'admin.html.twig' %}

{% block body %}
    <div class="col-lg-8 col-md-offset-1" >
    <h1>Actualite creation</h1>
    {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-md btn-primary" value="Create" style="float:right;" />
    {{ form_end(form) }}

    <ul class="list-unstyled">
        <li>
            <a class="link" href="{{ path('actualite_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
{% endblock %}
