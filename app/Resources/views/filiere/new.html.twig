{% extends 'admin.html.twig' %}

{% block body %}
<div class="col-lg-4 col-lg-offset-1">
    <h1>filiere creation</h1>
    {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create"  class="btn btn-md btn-primary" style="float:right; padding-right:20px; padding-left: 20px; margin-left: 10px;" />
    {{ form_end(form) }}

    <ul class="list-unstyled">
        <li>
            <a href="{{ path('filiere_index') }}" >Back to the list</a>
        </li>
    </ul>
</div>
{% endblock %}
