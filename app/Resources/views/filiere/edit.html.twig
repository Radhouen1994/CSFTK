{% extends 'admin.html.twig' %}

{% block body %}
    {% form_theme edit_form 'bootstrap_3_layout.html.twig' %}
<div class="col-lg-5 col-lg-offset-1">
    <h1>filiere edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="btn btn-md btn-primary" style="float:right; padding-right:20px; padding-left: 20px; margin-left: 10px;"/>
    {{ form_end(edit_form) }}
    {{ form_start(delete_form) }}
    <input type="submit" value="Delete" class="btn btn-md btn-danger" style="float:right; padding-right:20px; padding-left: 20px; margin-left: 10px;">
    {{ form_end(delete_form) }}
    <div class="row" style="padding: 10px;"></div>
    <ul class="list-unstyled">
        <li>
            <a href="{{ path('filiere_index') }}">Back to the list</a>
        </li>
        <li>

        </li>
    </ul>
</div>
{% endblock %}
